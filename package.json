{
  "name": "zero-to-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon monorepo: Rust backend + Next.js frontend.",
  "scripts": {
    "dev": "pnpm --filter nextjs-explorer dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  }
}
