{
  "name": "zero-to-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon monorepo: Rust backend + Next.js frontend.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun --filter nextjs-explorer dev",
    "build": "bun --filter '*' build",
    "lint": "bun --filter '*' lint"
  },
  "engines": {
    "bun": ">=1.3"
  }
}
